# condor/runs/

Generated campaign directories (`<tag>/` with JDLs, input lists, worker
scripts, and Condor logs) are written here by `../submit_batches.py`.
They are git-ignored; this README just keeps the directory in git.
